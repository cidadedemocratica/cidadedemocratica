<h1>Nova tag</h1>

<%= form_for(@tag, :url => admin_tags_path) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Salvar" %> ou
    <%= link_to "Voltar", admin_tags_path %>
  </p>
<% end %>