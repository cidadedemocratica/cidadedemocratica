<div class="grid_10 alpha">
  <h3>Mude seu avatar</h3>
  <div class="rounded">
    <%= form_for :imagem,
              :url => { :controller => "avatar",
                        :action => "create"
                      },
              :html => { :multipart => true } do |f| %>
    <%= hidden_field_tag "user[id]", @user.id %>
    <%= render :partial => "imagens/form_imagem", :locals => { :f => f } %>
    <p><%= submit_tag 'Upload!', :class => 'btn btn-mini' %> ou <%= link_to "Cancelar", :controller => "users", :action => "show", :id => @user.id %></p>
  </div>
</div>
<div class="grid_6 omega">
  <div class="dica_upload rounded rounded_beige">
  <h4>Dicas para subir seu avatar</h4>
  <p> - Use imagens (.jpg, .gif, .png, .bmp), de preferência com baixa resolução.</p>
  <p> - Sua imagem pode estar no formato retrato ou paisagem </p>
  <p> - No próximo passo, você selecionará a área da imagem que comporá seu avatar.</p>
  </div>
</div>
<% end %>
